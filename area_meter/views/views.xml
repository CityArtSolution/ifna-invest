<odoo>
    <data>

        <record model="ir.ui.view" id="rent_product_product_inherit_area">
            <field name="name">product_product</field>
            <field name="model">product.product</field>
            <field name="inherit_id" ref="renting.rent_product_product_inherit"/>
            <field name="arch" type="xml">
                <xpath expr="//field[@name='entry_number']" position="before">
                    <field name="rent_unit_area"/>
                    <field name="rent_unit_area_price"/>
                </xpath>
                <xpath expr="//field[@name='rental_pricing_ids']" position="replace">
                    <field name="rental_pricing_ids" nolabel="1"
                           context="{'default_product_template_id': active_id}">
                        <tree editable="bottom">
                            <control>
                                <create name="add_rental_pricing" string="Add a price"/>
                            </control>
                            <field name="product_variant_ids" widget="many2many_tags"
                                   groups="product.group_product_variant"
                                   domain="[('product_tmpl_id', '=', parent.id)]"
                                   attrs="{'column_invisible': [('parent.product_variant_count', '&lt;', 2)]}"/>
                            <field name="duration"/>
                            <field name="unit"/>
                            <field name="rent_unit_area"/>
                            <field name="rent_unit_area_price"/>
                            <field name="price" groups="area_meter.rental_pricing_manager"/>
                            <field name="pricelist_id" groups="product.group_product_pricelist"/>
                            <field name="product_template_id" invisible="1"/>
                            <field name="service_ids" widget="many2many_tags"
                                   context="{'default_product_id': product_template_id}"/>
                            <field name="currency_id" invisible="1"/>
                        </tree>
                    </field>
                </xpath>
            </field>
        </record>

        <record model="ir.ui.view" id="rent_product_inherit_area">
            <field name="name">product_template</field>
            <field name="model">product.template</field>
            <field name="inherit_id" ref="renting.rent_product_inherit"/>
            <field name="arch" type="xml">
                <xpath expr="//field[@name='rental_pricing_ids']" position="replace">
                    <field name="rental_pricing_ids" nolabel="1"
                           context="{'default_product_template_id': active_id}">
                        <tree editable="bottom">
                            <control>
                                <create name="add_rental_pricing" string="Add a price"/>
                            </control>
                            <field name="product_variant_ids" widget="many2many_tags"
                                   groups="product.group_product_variant"
                                   domain="[('product_tmpl_id', '=', parent.id)]"
                                   attrs="{'column_invisible': [('parent.product_variant_count', '&lt;', 2)]}"/>
                            <field name="duration"/>
                            <field name="unit"/>
                            <field name="rent_unit_area"/>
                            <field name="rent_unit_area_price"/>
                            <field name="price" groups="area_meter.rental_pricing_manager"/>
                            <field name="pricelist_id" groups="product.group_product_pricelist"/>
                            <field name="product_template_id" invisible="1"/>
                            <field name="service_ids" widget="many2many_tags"
                                   context="{'default_product_id': product_template_id}"/>
                            <field name="currency_id" invisible="1"/>
                        </tree>
                    </field>
                </xpath>

                <xpath expr="//field[@name='entry_number']" position="before">
                    <field name="rent_unit_area"/>
                    <field name="rent_unit_area_price"/>
                </xpath>
            </field>
        </record>


        <record model="ir.ui.view" id="rent_sale_order_inherit_form_area">
            <field name="name">sale_order</field>
            <field name="model">sale.order</field>
            <field name="inherit_id" ref="sale_renting.rental_order_primary_form_view"/>
            <field name="arch" type="xml">
                <xpath expr="//field[@name='order_line']/tree/field[@name='is_rental']"
                       position="after">
                    <field name="rent_unit_area" optional="hide"/>
                    <field name="rent_unit_area_price" optional="hide"/>
                </xpath>
            </field>
        </record>

    </data>
</odoo>